name: Docker push to Hub
on:
  push:
    branches: [newbranch]
  pull_request:
    branches: [newbranch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Docker build 
        run: |
          docker build -t dockertp ../
